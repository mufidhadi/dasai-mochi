services:
  mochi-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${CONTAINER_NAME:-dasai-mochi}
    restart: unless-stopped
    networks:
      - web_proxy
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1/ || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web_proxy"
      - "traefik.http.routers.dasai-mochi.entrypoints=websecure"
      - "traefik.http.routers.dasai-mochi.rule=Host(`${APP_DOMAIN:-mochi.masmuf.cloud}`)"
      - "traefik.http.routers.dasai-mochi.tls.certresolver=${TRAEFIK_CERTRESOLVER:-myresolver}"
      - "traefik.http.services.dasai-mochi.loadbalancer.server.port=80"

networks:
  web_proxy:
    external: true
