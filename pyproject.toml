[project]
name = "dasai-mochi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mufid", email = "mufidhadi@madhani.id" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
dasai-mochi = "dasai_mochi:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
